# GST Intelligence RL Environment — Dependencies
# Designed for: 2 vCPU, 8 GB RAM (hackathon constraint)
# Python: 3.11+

# ─── Core RL Framework ───────────────────────────────────────────────────────
gymnasium>=0.29.1           # OpenAI Gym successor (OpenEnv compliant)
stable-baselines3>=2.2.1    # PPO, DQN implementations
torch>=2.1.0                # SB3 dependency (CPU only — fits in 8GB)

# ─── API & LLM ───────────────────────────────────────────────────────────────
openai>=1.30.0              # OpenAI client (MANDATORY per hackathon)

# ─── Pydantic (typed models) ─────────────────────────────────────────────────
pydantic>=2.5.0             # GSTObservation, GSTAction, RewardSignal

# ─── Data Generation ─────────────────────────────────────────────────────────
faker>=22.0.0               # Synthetic invoice generation (en_IN locale)
numpy>=1.26.0
rapidfuzz>=3.6.0            # Fuzzy invoice number matching for ITC reconciliation

# ─── Utilities ───────────────────────────────────────────────────────────────
python-dateutil>=2.8.2
pyyaml>=6.0.1               # openenv.yaml parsing

# ─── Web Server (HuggingFace Space API endpoint) ─────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# ─── UI ──────────────────────────────────────────────────────────────────────
gradio>=4.44.0
pandas>=2.0.0

# ─── OpenEnv CLI (for openenv validate) ──────────────────────────────────────
# openenv-cli>=0.1.0        # Uncomment if available on PyPI at submission time

# ─── Dev / Testing (not needed in Docker) ────────────────────────────────────
# pytest>=7.4.0
# black>=23.0